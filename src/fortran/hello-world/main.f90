function greet(name)
	greet = "Hello, World!"
return
end function greet

program hello
    ! This is a comment line; it is ignored by the compiler
    write(*,'(a)') adjustl(trim('Hello, World!'))
end program hello
