load("@bazel_gazelle//:def.bzl", "gazelle")
load("@com_github_bazelbuild_buildtools//buildifier:def.bzl", "buildifier")

buildifier(
    name = "buildifier",
    lint_mode = "fix",
    lint_warnings = ["all"],
)

# gazelle:prefix github.com/satreix/everest
gazelle(
    name = "gazelle",
    mode = "fix",
)
